---
const { post } = Astro.props;
---

<a href={`/blog/${post.slug}`} class="block bg-white shadow-lg rounded-lg overflow-hidden">
  <img src={post.data.image} alt={post.data.title} class="w-full h-48 object-cover" />
  <div class="p-4">
    <h2 class="text-xl font-bold">{decodeURIComponent(post.data.title)}</h2>
    <p class="text-sm text-gray-500">{post.data.date}</p>
    <p class="mt-2">{decodeURIComponent(post.data.description)}</p>
  </div>
</a>